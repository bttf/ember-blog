import {
  markdownToHtml
} from 'frontend/helpers/markdown-to-html';

module('MarkdownToHtmlHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = markdownToHtml(42);
  ok(result);
});
